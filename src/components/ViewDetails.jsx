const ViewDetails = ({ onViewDetail }) => {
  return (
    <>
      <button className="text-green-600" onClick={onViewDetail}>
        View Detials →
      </button>
    </>
  );
};

export default ViewDetails;
